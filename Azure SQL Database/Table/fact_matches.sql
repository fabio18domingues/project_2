CREATE TABLE dbo.fact_matches (
    ID_MATCH INT PRIMARY KEY,
    MATCH_DATE DATE,
    ID_DATE INT,
    MATCH_TIME TIME,
    ID_TIME INT,
    MATCH_DATETIME DATETIME,
    ID_DATETIME BIGINT,
    ID_HOME_TEAM INT,
    ID_AWAY_TEAM INT,
    ID_LEAGUE INT,
    FULL_TIME_HOME_GOAL INT,
    FULL_TIME_AWAY_GOAL INT,
    FULL_TIME_RESULT VARCHAR(5),
    HALFTIME_HOME_GOAL INT,
    HALFTIME_AWAY_GOAL INT,
    HALFTIME_RESULT VARCHAR(5),
    HOME_SHOTS INT,
    AWAY_SHOTS INT,
    HOME_SHOTS_ON_TARGET INT,
    AWAY_SHOTS_ON_TARGET INT,
    HOME_FOULS INT,
    AWAY_FOULS INT,
    HOME_CORNERS INT,
    AWAY_CORNERS INT,
    HOME_YELLOW INT,
    AWAY_YELLOW INT,
    HOME_RED INT,
    AWAY_RED INT
);

